
/* 
TODO: all of this.

It'll be a lot easier to write tests once the library can take streams
other than the default stdin/stdout. 
So do that then write the tests.

var assert = require("assert");

var cmd = require('..');

describe ('cmdy', function() {
  it ('something', function() {
  });
});

*/

